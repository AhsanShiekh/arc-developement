import { makeStyles } from "@material-ui/core";

const WebsitePageStyles = makeStyles((theme) => ({
  root: {
    width: "100vw",
  },
  cards: {
    width: "100vw",
  },
}));

export default WebsitePageStyles;
